from django.db import models

'''
class login(models.Model):
    e_mail = models.EmailField(max_length=50, null=False, blank=False)
    password = models.
   login_date = models.DateTimeField(auto_now=True)
'''